database:
  dsn: file::memory:?cache=shared

queries:
  - type: one
    path: /user
    mock:
      object_js: |
        return { id: parseInt(input.id), name: "Mock User", email: "mock@example.com" };
