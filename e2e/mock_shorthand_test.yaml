queries:
  # Array shorthand for type: many
  - type: many
    path: /users
    mock:
      array:
        - { id: 1, name: Alice }
        - { id: 2, name: Bob }

  # Object shorthand for type: one
  - type: one
    path: /user
    mock:
      object: { id: 42, name: Charlie }
