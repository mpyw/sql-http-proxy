dsn: postgres://localhost/test

mutations:
  - type: one
    method: POST
    path: /json-only
    accepts: [json]
    mock:
      object: { received: true }

  - type: one
    method: POST
    path: /form-only
    accepts: [form]
    mock:
      object: { received: true }
