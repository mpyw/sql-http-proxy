database:
  dsn: postgres://localhost/test

queries:
  - type: one
    method: POST
    path: /search
    mock:
      object: { id: 1 }
    transform:
      post: |
        return { query: input.query, result: output }

  - type: one
    method: GET
    path: /get-only
    mock:
      object: { id: 1 }
