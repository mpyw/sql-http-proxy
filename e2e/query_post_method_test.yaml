dsn: postgres://localhost/test

queries:
  - type: one
    method: POST
    path: /search
    sql: SELECT 1
    transform:
      mock: { id: 1 }
      post: |
        return { query: input.query, result: output }

  - type: one
    method: GET
    path: /get-only
    sql: SELECT 1
    transform:
      mock: { id: 1 }
