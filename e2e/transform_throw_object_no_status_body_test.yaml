database:
  dsn: ":memory:"

queries:
  - type: one
    path: /obj-error
    sql: SELECT * FROM users WHERE id = :id
    transform:
      pre: |
        throw { foo: "bar", message: "custom object" };
