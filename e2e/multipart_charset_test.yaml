dsn: postgres://localhost/test

mutations:
  - type: one
    method: POST
    path: /multipart
    sql: SELECT 1
    transform:
      mock: { received: true }
      post: |
        return { name: input.name }
