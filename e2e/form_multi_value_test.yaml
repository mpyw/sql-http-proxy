database:
  dsn: postgres://localhost/test

mutations:
  - type: one
    method: POST
    path: /form
    mock:
      object: { received: true }
    transform:
      post: |
        return { tags: input.tags, name: input.name }
