database:
  dsn: file::memory:?cache=shared

queries:
  - type: one
    path: /user
    mock:
      object_js: |
        if (input.id === "999") {
          return null;
        }
        return { id: parseInt(input.id), name: "Mock User" };
