dsn: file::memory:?cache=shared

queries:
  - type: many
    path: /users
    sql: SELECT * FROM users
    transform:
      mock: |
        return [
          { id: 1, name: "User 1" },
          { id: 2, name: "User 2" },
          { id: 3, name: "User 3" }
        ];
