database:
  dsn: "sqlite::memory:"

queries:
  - type: one
    path: /user
    sql: SELECT * FROM users WHERE id = :id
    transform:
      post: |
        throw { body: { message: "no status specified in post" } };
